#include <stdio.h>

int main() {
    for (int i = 1; ; i++) {
        printf("%d\n", i);
        if (i % 9 == 0) {
            printf("Found a multiple of 9, stopping the program.\n");
            break; 
        }
    }

    return 0;
}
