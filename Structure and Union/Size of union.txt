#include <stdio.h>

union SampleUnion {
    int integer;
    float floatingPoint;
    char character;
};

int main() {
    union SampleUnion sample;

    printf("Size of the union: %lu bytes\n", sizeof(sample));

    return 0;
}
